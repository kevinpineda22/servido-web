'https://servido-web-f2kr.onrender.com/save-activity' : Ruta del servidor Render


const filePath = path.join('C:', 'Users', 'USER', 'Documents', 'libro.xlsx'); : Ruta real del archivo excel

'http://localhost:3000/save-activity' : Ruta de fetch para el servidor local

const filePath = path.join('C:', 'Users', 'USER', 'Documents', 'pruebassss.xlsx'); : Ruta carpeta de excel

const filePath = path.join('C:', 'Users', 'Desarrollo', 'Documents', 'RegistroOperaciones.xlsx');

 console.log(`Servidor corriendo en http://192.168.1.200:${PORT}`);